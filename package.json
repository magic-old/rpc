{
  "name": "@magic/rpc",
  "version": "0.0.1",
  "main": "./src/index.js",
  "scripts": {
    "start": "NODE_ENV=production nyc -a test",
    "test": "nyc -a test",
    "format": "format --write",
    "format:check": "format --list-different"
  },
  "dependencies": {
    "grpc": "1.10.1"
  },
  "devDependencies": {
    "@magic/lint": "github:magic/lint",
    "@magic/test": "github:magic/test"
  }
}
