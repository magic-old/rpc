{
  "name": "@magic/rpc",
  "version": "0.0.1",
  "main": "./src/index.js",
  "scripts": {
    "start": "npm run format:check && nyc -a node test",
    "test": "npm run format:check && VERBOSE=true nyc -a node test",
    "format": "format --write",
    "format:check": "format --list-different"
  },
  "dependencies": {
    "grpc": "1.10.1"
  },
  "devDependencies": {
    "@magic/lint": "github:magic/lint",
    "@magic/test": "github:magic/test",
    "nyc": "11.6.0"
  }
}
